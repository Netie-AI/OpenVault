# Week ship plan — Route + Netie Clicks

Updated: 2026-07-26

## Locations

| Product | Path |
|---|---|
| **Netie Clicks** (standalone) | `D:\Netie Clicks` |
| OpenVault (custody + Route + Ship) | `D:\OpenVault` |
| Clicky MIT vendor | `D:\OpenVault\vendor\clicky` |

`apps/click` was **removed** from OpenVault (stub `MOVED.md` if Windows left a lock).

## OpenVault — done

- Route package + router mounted (`/api/route/*`) — 14 tests
- Route UI tabs on `/proxy`

## Netie Clicks — week 1

- Ctrl+Space → frame drag → OpenVault vision chat
- 60s hot memory (in-process; Redis when `OPENVAULT_REDIS_URL` set)
- See `D:\Netie Clicks\FULL_PLAN.md`
