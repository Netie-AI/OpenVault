---
keywords: EPIC, Mode B, completeness, #15, #16, #17, #56, OpenShip, FreeRoute, openvault app, sealed refuse
main_idea: Close an epic from live buyer artifacts, not CLOSED child tickets. Do not close a parent while a child is OPEN. Sealed refuse on /freeroute Send counts for FreeRoute UI completeness.
---

# EPIC Mode B -- #15 #16 COMPLETE; #17 waits #56

PREFLIGHT: HIT. Reused `2026-09-08_epic-reopen-17-16.md`.

## Completeness rule (this run)

A checked box and a CLOSED child are not evidence. Re-hit:

1. `GET /api/ship/targets` (and the `:3010/ov-api` rewrite) for missing `openship_cloud` / `openship.io` / `OPENSHIP_URL`.
2. Browser HTML on `:3010/ship` (AppBar + Ship heading + Choose folder).
3. Browser Send on `:3010/freeroute` posting `/ov-api/v1/chat/completions`. Explicit sealed refuse counts. Pytest-only does not.
4. Do not close the parent epic while any child ticket is OPEN. #17 waits live `openvault app` (#56).

## Commands this session

- Pages `/` `/vault` `/ship` `/freeroute` `/gate`: HTTP 200 `text/html` under 8s.
- Targets ids: `spaceship_ftp,cloudflare_pages,coolify,netlify,vps_ssh,aws_guide,local_demo`. Three vendor needles MISS.
- Chat: `POST :5000/v1/chat/completions` 403 `openvault_vault_sealed`. Same via `:3010/ov-api` with Origin. Playwright Send showed the refuse text.
- `cd OpenMW && uv run pytest tests/test_ship_cloud.py tests/test_ship_engine.py -q` -- 16 passed.

Not closed: #17. Remaining: live `openvault app` while Electron can own `:3010`.
