---
keywords: EPIC, F32, reopen, #17, #16, #15, #55, #56, #57, #58, #59, compiling-proxy, OpenShip, FreeRoute, READY
main_idea: Mode A after F32. Reopened #17+#16 in flight and #15 queued. Filed #55-#59. Ticket Runner starts at #55 GUI restore. Completeness is browser HTML / no vendor OpenShip card, not pytest.
---

# EPIC Agent -- F32 reopen #17 #16 and queue #15

PREFLIGHT: HIT. Reused `2026-09-08_prd-gui-freeroute-openship.md`. No extra spawn.

## What the previous batch missed (decomposition)

#17 closed 2026-08-06 from pytest+tsx while live `:3010` still hangs. This session `Invoke-WebRequest http://127.0.0.1:3010/` timed out at 8s. CHANGELOG 2026-09-04 already recorded HT3 using the API because Compiling proxy never finished. Checkbox COMPLETE was not the buyer artifact.

#16 closed from adapter pytest (#27-#30) while `openship_client.py` + `openship_cloud` / openship.io remained a product path. DR-0003 forbids embedding vendor runtime. Hosts under `ship/hosts/` were already more than Cloudflare Pages.

#15 API EARS hold (`POST /v1/chat/completions` 200). TAS-OPENVAULT playground row is false: no `/playground` page, no `FreeRouteSmoke`. Remainder is buyer chat on `:3010`, queued until GUI serves.

## Board (2026-09-08)

| Issue | State | Role |
|---|---|---|
| https://github.com/Netie-AI/OpenVault/issues/17 | OPEN | EPIC-OV-WEB visible WIP |
| https://github.com/Netie-AI/OpenVault/issues/16 | OPEN | EPIC-OV-FREEBUILD second WIP |
| https://github.com/Netie-AI/OpenVault/issues/15 | OPEN | EPIC-OV-FREEROUTE QUEUED |
| https://github.com/Netie-AI/OpenVault/issues/55 | OPEN READY | FIRST -- restore `:3010` HTML |
| https://github.com/Netie-AI/OpenVault/issues/56 | OPEN | desktop `openvault app`; blocked on #55 |
| https://github.com/Netie-AI/OpenVault/issues/57 | OPEN READY | retire vendor OpenShip (no GUI) |
| https://github.com/Netie-AI/OpenVault/issues/58 | OPEN | `/ship` UI; blocked on #55+#57 |
| https://github.com/Netie-AI/OpenVault/issues/59 | OPEN | FreeRoute playground; blocked on #55; parent queued |

Not reopened: #18 (HT1-HT5 CLEARED), #12, #13, #14, #33, #48, #38, #50-as-parent.

## Completeness (re-derive later from code)

- **#17 COMPLETE** when curl/browser 200 `text/html` under 8s on `/`, `/vault`, `/ship`, `/freeroute`, `/gate`, and `openvault app` shows that UI after `:5000/api/healthz` 200.
- **#16 COMPLETE** when `GET /api/ship/targets` has no `openship_cloud` / openship.io / OPENSHIP_URL card, engine does not call the vendor client, simulate never fakes a URL, and `/ship` works in a browser after #55.
- **#15 COMPLETE** when a `:3010` prompt hits existing `POST /v1/chat/completions` and shows a completion or explicit refuse. Do not pull F15.

## Ticket Runner

Execute **#55 first**. Optional second seat: **#57** (backend only). Do not seat #59 until #55 is verified from a browser.
