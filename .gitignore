# Python artifacts
__pycache__/
*.py[cod]
*.pyo
*.pyd

# Virtual environments
.venv/
venv/

# Coverage
htmlcov/
.coverage
coverage.xml

# Tool caches
.ruff_cache/
.mypy_cache/
.pytest_cache/

# Build artifacts
*.egg-info/
build/
dist/

# Environment files
.env
openvault.local.json

# Local OpenVault runtime (keys, mesh, connect pack)
.openvault/
**/.openvault/

# Local reports
reports/
openmw_demo_ui/

# IDE files
.vscode/
.idea/

# Node / Next build output
node_modules/
.next/
.turbo/
*.tsbuildinfo

# Upstream clones we build on top of (re-clone by hand into vendor/<name> when needed —
# no vendor-sync script exists yet). Code we actually keep lives in apps/, copied out of
# here deliberately.
vendor/
