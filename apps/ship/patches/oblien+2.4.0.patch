diff --git a/dist/http.js b/dist/http.js
index 9af485b975fb0b5711f8c4b224562da2ee249bc6..5605878be6b62c8ebceac9d76f56efd72ef77914 100644
--- a/dist/http.js
+++ b/dist/http.js
@@ -69,6 +69,7 @@ export class HttpClient {
         const init = {
             method: options.method,
             headers: { ...this.headers, ...options.headers, 'Accept': 'text/event-stream' },
+            signal: options.signal,
         };
         if (options.body !== undefined) {
             init.body = JSON.stringify(options.body);
@@ -105,6 +106,7 @@ export class HttpClient {
             }
         }
         finally {
+            await reader.cancel().catch(() => {});
             reader.releaseLock();
         }
     }
diff --git a/dist/resources/logs.d.ts b/dist/resources/logs.d.ts
index 054c499ea5523d3059290d0212535a2e17bc4c06..de5dfb8e5f9971fd3a3025a44662c2fc23792f16 100644
--- a/dist/resources/logs.d.ts
+++ b/dist/resources/logs.d.ts
@@ -6,7 +6,7 @@ export declare class LogsResource {
     private readonly _http;
     constructor(_http: HttpClient);
     /** Get logs. */
-    get(workspaceId: string, params?: LogsParams): Promise<ApiResponse>;
+    get(workspaceId: string, params?: LogsParams, options?: { signal?: AbortSignal }): Promise<ApiResponse>;
     /** Clear logs. */
     clear(workspaceId: string): Promise<ApiResponse>;
     /** List available log files. */
@@ -20,6 +20,7 @@ export declare class LogsResource {
     /** Stream command logs (SSE). */
     streamCmd(workspaceId: string, params?: {
         tail_lines?: number;
+        signal?: AbortSignal;
     }): AsyncGenerator<LogStreamEvent>;
 }
 //# sourceMappingURL=logs.d.ts.map
\ No newline at end of file
diff --git a/dist/resources/logs.js b/dist/resources/logs.js
index 0f0530a6ae74ce982b8cfe61ae7bb14d7348948a..a5dfa4da3a0973380b1110cdc929d3dbc915ef0c 100644
--- a/dist/resources/logs.js
+++ b/dist/resources/logs.js
@@ -5,11 +5,12 @@ export class LogsResource {
         this._http = _http;
     }
     /** Get logs. */
-    async get(workspaceId, params = {}) {
+    async get(workspaceId, params = {}, options) {
         return this._http.request({
             method: 'GET',
             path: `/workspace/${workspaceId}/logs`,
             query: params,
+            signal: options?.signal,
         });
     }
     /** Clear logs. */
@@ -44,6 +45,7 @@ export class LogsResource {
         yield* this._http.requestSSE({
             method: 'GET',
             path: `/workspace/${workspaceId}/logs/stream/cmd`,
+            signal: params?.signal,
             query: params?.tail_lines ? { tail_lines: params.tail_lines } : undefined,
         });
     }
diff --git a/dist/resources/workloads.d.ts b/dist/resources/workloads.d.ts
index 2a598f4c8db8f21192612755528cc18c2835e375..d777e5e0297290088c41c1316123a3fbd93f020c 100644
--- a/dist/resources/workloads.d.ts
+++ b/dist/resources/workloads.d.ts
@@ -26,13 +26,13 @@ export declare class WorkloadsResource {
     /** Get workload status. */
     status(workspaceId: string, workloadId: string): Promise<ApiResponse>;
     /** Get workload logs. */
-    logs(workspaceId: string, workloadId: string): Promise<ApiResponse>;
+    logs(workspaceId: string, workloadId: string, options?: { signal?: AbortSignal }): Promise<ApiResponse>;
     /** Get workload stats. */
     stats(workspaceId: string, workloadId: string): Promise<ApiResponse>;
     /** Stream workload stats (SSE). */
     statsStream(workspaceId: string, workloadId: string): AsyncGenerator<WorkloadStatsEvent>;
     /** Stream workload logs (SSE). */
-    logsStream(workspaceId: string, workloadId: string): AsyncGenerator<WorkloadLogEntry>;
+    logsStream(workspaceId: string, workloadId: string, options?: { signal?: AbortSignal }): AsyncGenerator<WorkloadLogEntry>;
     /** Stream stats for ALL workloads (SSE). */
     allStatsStream(workspaceId: string): AsyncGenerator<WorkloadStatsEvent>;
 }
diff --git a/dist/resources/workloads.js b/dist/resources/workloads.js
index 3cf3ba46157c9cf94f1adebe5a7742433c8bdd4e..335e79b0e1c8b6b852091ee50ced1c66e3939ba7 100644
--- a/dist/resources/workloads.js
+++ b/dist/resources/workloads.js
@@ -51,8 +51,8 @@ export class WorkloadsResource {
         return this._http.request({ method: 'GET', path: `/workspace/${workspaceId}/workloads/${workloadId}/status` });
     }
     /** Get workload logs. */
-    async logs(workspaceId, workloadId) {
-        return this._http.request({ method: 'GET', path: `/workspace/${workspaceId}/workloads/${workloadId}/logs` });
+    async logs(workspaceId, workloadId, options) {
+        return this._http.request({ method: 'GET', path: `/workspace/${workspaceId}/workloads/${workloadId}/logs`, signal: options?.signal });
     }
     /** Get workload stats. */
     async stats(workspaceId, workloadId) {
@@ -66,10 +66,11 @@ export class WorkloadsResource {
         });
     }
     /** Stream workload logs (SSE). */
-    async *logsStream(workspaceId, workloadId) {
+    async *logsStream(workspaceId, workloadId, options) {
         yield* this._http.requestSSE({
             method: 'GET',
             path: `/workspace/${workspaceId}/workloads/${workloadId}/logs/stream`,
+            signal: options?.signal,
         });
     }
     /** Stream stats for ALL workloads (SSE). */
