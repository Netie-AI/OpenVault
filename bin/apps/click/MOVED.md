# Moved

**Netie Clicks** is a standalone app:

```text
D:\Netie Clicks
```

```powershell
cd "D:\Netie Clicks"
npm start
```

Hotkey: **Ctrl+Space** → drag frame → chat.

This OpenVault folder is only a redirect stub (Windows file lock during migration).
Delete `apps/click` when nothing has it open.
