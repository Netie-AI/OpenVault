This repo's law lives in `CLAUDE.md` — read it before acting on anything here.
implementation_plan.md moved to `docs/reference/nvme-sentinel-spec.md`; it is still the
source of truth for NVMe field offsets/opcodes cited as "§4" in code comments.
